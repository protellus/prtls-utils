from django.apps import AppConfig

# This is the configuration class for the prtls_utils app.

class PrtlsUtilsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'prtls_utils'
